---
:concurrency: <%= ENV.fetch("SIDEKIQ_CONCURRENCY", 5) %>
:timeout: 25
:queues:
  - [critical, 3]
  - [default, 2]
  - [low, 1]

production:
  :concurrency: <%= ENV.fetch("SIDEKIQ_CONCURRENCY", 3) %>

staging:
  :concurrency: <%= ENV.fetch("SIDEKIQ_CONCURRENCY", 3) %>
